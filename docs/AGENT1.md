# AGENT1.md

## Folder role

`docs/` is the human-readable explanatory layer for Agent 1 scope.

This folder should explain:

- repository role,
- architectural meaning,
- integration logic,
- upstream distinctions,
- validation limits.

---

## Agent 1 scope in this folder

Agent 1 may use `docs/` for:

- index and architecture documents,
- upstream integration notes,
- shell-versus-engine documentation,
- explicit statements of what is canonical, imported, analogical, or provisional.

---

## Rules

- `docs/INDEX.md` should remain the human-facing navigation entry.
- Any new machine registry object with human meaning should eventually appear in docs.
- Imported shell objects must be described as imported, not native canonical derivations.
- If a document is analogical or non-probative, it must say so.
