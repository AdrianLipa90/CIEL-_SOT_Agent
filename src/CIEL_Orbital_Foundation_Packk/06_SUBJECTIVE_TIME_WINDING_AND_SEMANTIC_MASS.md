# Subjective Time, Winding, and Semantic Mass

## Semantic mass

Semantic mass is not file size, complexity, or popularity.
It is the effective resistance and gravitational significance of an entity in the orbital state field.

## Initial operator

Use:
`M_sem(f) = α M_EC(f) + β M_ZS(f) + χ C_dep(f) + δ C_prov(f) + ε C_exec(f)`

Where:
- `M_EC` = iterative-topological weight
- `M_ZS` = spectral-resonant weight
- `C_dep` = dependency centrality
- `C_prov` = provenance centrality / canonicality
- `C_exec` = execution criticality

## Subjective time

Every entity must have a local time scale.

Use:
`Δτ_i(k) = Δt · g(r_i(k), C_i(k), Δ_i(k), m_i(k), A_i(k))`

Inputs:
- orbital radius
- local coherence
- local defect
- semantic mass
- local attractor influence

Outputs:
- local pace of experienced evolution
- scaling of winding accumulation
- update cadence suggestion

## Winding

Winding must come from dynamics.

Use:
`w_i(N) = (1 / 2π) Σ Δφ_i(k) · (Δt / Δτ_i(k))`

Recommended winding components:
- `w_ec`
- `w_zs`
- `w_rel`
- `w_red`

Total winding:
`w_total = w_ec + w_zs + w_rel + w_red`

## Orbit assignment

Initial effective rule:
`T_i^2 ∝ a_i^3 / A_eff`

Where:
- `T_i` = effective period
- `a_i` = orbital radius / semi-major orbital indicator
- `A_eff` = effective sphere attractor weight

This is a discrete effective law, not a literal transplant of celestial mechanics.
