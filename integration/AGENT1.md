# AGENT1.md

## Folder role

`integration/` is the machine-readable and report-oriented integration layer.

This is the main operational workspace of Agent 1.

---

## Agent 1 scope in this folder

Agent 1 may use this folder for:

- repository registry,
- coupling maps,
- upstream declarations,
- machine index registry,
- integration reports,
- shell-to-engine boundary mappings.

---

## Rules

- Every imported object should carry explicit provenance.
- Placeholder versus canonical status must remain visible.
- `ciel-omega-demo` must be treated here as shell/cockpit upstream, not engine.
- Future `Informational Dynamics` engine mappings should be introduced explicitly, not implicitly.
