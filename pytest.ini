[pytest]
addopts = -q
# Root-level canonical test collection only.
# Donor/import mirrors are intentionally excluded from default discovery.
testpaths =
    tests
    src/ciel_rh_control_mini_repo/tests
    src/CIEL_OMEGA_COMPLETE_SYSTEM/ciel_omega
